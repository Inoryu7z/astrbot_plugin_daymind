### v1.8.5

**🐛 修复日记写入 livingmemory 的 umo 归属错乱**

* 日记写入 livingmemory 时，归属 umo 改为优先取推送目标列表首项。
* 未配置推送目标时维持原逻辑。

---

### v1.8.4

**🐛 修复主动对话未生效 + 下调日记重要性**

* 修复思考后主动对话从未触发的问题。
* 主动对话检测到工具调用后立即返回，不再重试。
* 日记存入 livingmemory 的重要性从 0.7 降为 0.4。

---

### v1.8.3

**🐛 修复日记存不进 livingmemory 的缓存失效问题**

* livingmemory 可用性改为每次实时探测，避免启动时误判为永久不可用。
* 增加对已关闭实例的识别。
* 存储失败后清空实例缓存，确保下次重试拿到最新实例。

---

### v1.8.2

**🛡️ 日记存入记忆系统增加重试与补存机制**

* 日记写入记忆系统新增自动重试，应对接口偶发失败。
* 新增本地待补存状态，写入失败时不进冷却。
* 新增每 60s 自动补存机制，跨天 reset 不清除待补存信息。

---

### v1.8.1

**🐛 适配 LivingMemory v2.0+ 新版 API**

* 适配新版 API 命名变更。
* 兼容新版返回类型。
* 仅影响旧日记的标记删除流程，不影响新日记存储。

---

### v1.8.0

**🗣️ 思考后主动对话**

* 新增思考后主动对话功能：思考时可触发向指定目标发送主动消息。
* 支持三档控制模式：关闭（默认）/ 低频 / 普通。
* 主动对话消息自动写入对话历史。
* 按推送目标设置冷却，防止频繁打扰（默认 90 分钟）。
* 新增人格级配置项：`proactive_chat_mode`、`proactive_chat_push_target`、`proactive_chat_cooldown_minutes`。

---

### v1.7.0

**🧠 思考模块适配 DayFlow 细分骨架**

* 思考提示词适配 DayFlow 细分骨架，从"复述日程"转向"写出活动中的具体经历"。
* 三档模式定义更新：简洁聚焦内心落点、适量写具体经历、丰富必须三维度。
* 新增增强版时段上下文。
* sub_events 为空时自动回退到原大时段格式。

---

### v1.6.3

**🐛 修复日程可能被误覆盖的问题**

* 修复日程数据瞬态异常时误判日程缺失并重新生成的问题。
* 新增二次验证，已有有效日程时跳过重新生成。

---

### v1.6.2

**🐛 修复自动补生成日程未持久化的问题**

* 修复自动补生成的日程未持久化的问题。

---

### v1.6.1

**🏗️ 架构重构 + 日记渲染优化 + 梦境历史清理**

**1. 🏗️ 架构重构**

* 调度模块拆分整理，主文件显著瘦身。

**2. 🖼️ 日记渲染优化**

* 优化日记图片背景渲染性能。
* 新增图片最大高度限制（4096px）。
* 新增字体下载镜像回退。
* 修复日记图片 footer 显示当前时间而非日记时间的问题。

**3. 🌙 梦境历史清理**

* 新增梦境历史清理机制，复用 `diary_retention_days` 配置。

---

### v1.6.0

**🖼️ 日记图片渲染：纸质手写风**

* 新增日记图片渲染功能：开启后日记以纸质手写风格图片发送。
* 纸质手写风设计：暖色米白纸张纹理 + 优雅日期标题 + 手写体正文。
* 智能字体方案：优先使用系统字体，找不到时自动下载开源中文字体。
* 支持手动日记、今日日记、昨日日记查看及自动推送的图片渲染。
* 渲染失败时自动回退到纯文字发送。
* 新增配置项：`enable_diary_image`（人格级，默认关闭）。
* 新增依赖：Pillow>=10.0。

---

### v1.5.2

**🔧 稳定性修复**

* 修复状态保存的竞态条件。
* 修复同步文件 I/O 阻塞事件循环的问题。
* 修复梦境记忆重复注入的风险。
* 修复未启用人格状态未清理的问题。
* 新增 `.gitignore` 排除缓存和本地交接文档。

---

### v1.5.1

**🧠 思考质量优化：日程对齐 + 差异化强制**

* 思考 prompt 仅传入当前时段±1个时段的日程，减少信息干扰。
* 新增【变】差异化格式：LLM 输出先写差异点再写正文，强制推进感。
* 重写默认 prompt 模板，日程对齐为最高优先级。
* 互动背景标注"仅作情绪参考"。
* 调整信息选择优先级：日程场景 > 身体感受/动作 > 情绪 > 对话残留 > 最近思考 > 外观。

---

### v1.5.0

**🌙 梦境构建系统**

* 新增梦境生成功能：静默时段 Bot 会生成象征性、情感驱动的梦境片段。
* 梦境基于入睡前心情、最后一条思考和对话余温生成。
* 每晚随机 1-2 个梦，入睡后 15 分钟开始，间隔至少 30 分钟。
* 醒来后梦境余韵以低强度影响心情状态。
* 醒来后首次对话时注入梦境记忆。
* 梦境历史持久化存储。
* 新增配置项：`enable_dream`、`dream_count_range`、`dream_provider_id`。
* 梦境 Prompt 与清醒思考完全独立。
* 新增公开接口供其他插件读取梦境数据。

---

### v1.4.2

**⚙️ 配置界面精简**

* 人格模板配置项按功能分组重排序。
* 新增「显示高级设置」开关，默认关闭。
* 14 个高级配置项按需展开，减少视觉负担。
* 优化部分 hint 描述。

---

### v1.4.1

**📓 手账主题差异化重构**

* 移除桌面模式中央的"人格笔记本"装饰。
* 桌面模式：便利条点击后直接在桌面右侧展开阅读卡片。
* 笔记本模式：左页改为日期目录导航，可独立选择任何一天。
* 两个模式完全独立可用。
* 全局字号再提升 1~2px。
* 便利条字号提升。

---

### v1.4.0

**📓 手账主题修复与优化**

* 全面提升手账主题字号。
* 便利条尺寸增大。
* 隐藏手账主题下不适用的星系元素。
* 侧边栏标题根据主题动态切换。
* 配置项"默认模式"新增 notebook/desktop 选项。
* 空状态提示文本根据主题显示对应措辞。
* 手账主题下 Hero 区域改为单列布局。

---

### v1.3.9

**📓 手账主题上线**

* 新增手账（Journal）主题，与星系主题并列为大主题。
* 手账主题包含两个模式：桌面模式、笔记本模式。
* 引入手写字体，离线回退到楷体。
* 便利条点击后自动切换到笔记本模式阅读。
* 手账主题下所有 UI 元素均有暖色系覆盖样式。
* 配置项 `webui_default_theme` 新增 `journal` 可选值。
* 主题切换时自动映射模式。

---

### v1.3.8

**🎨 WebUI 体验优化**

* 密码输入改为自定义毛玻璃弹窗。
* 实现真正的 Toast 通知系统。
* 阅读台加载时显示遮罩 + Spinner。
* 移动端添加侧边栏折叠功能。
* 搜索图标替换为 SVG 放大镜图标。
* 添加 `prefers-reduced-motion` 无障碍媒体查询。
* 观测/星图模式切换添加渐入过渡动画。
* 添加 Firefox 滚动条样式与焦点状态。
* Hero 装饰点添加浮动微动画。

---

### v1.3.7

**🐛 修复 Provider 获取逻辑 & 日记 Fallback 机制**

* 修复 Provider 获取不可靠的问题。
* 日记生成支持 provider fallback：配置的 `diary_provider_id` 失败时自动回退到默认 provider。

---

### v1.3.6

**🌙 智能静默时段（DayFlow 联动）**

* 新增「智能静默时段」配置项 `smart_silent_hours`，自动从 DayFlow 日程提取睡眠时段。
* 仅当第一个时段起始时间在 06:00 之前、结束时间不超过 12:00 时才采纳。
* 提取失败时自动回退到手动配置的 `silent_hours_end`。
* 提取结果按日期缓存。
* 静默状态接口新增相关字段，方便 WebUI 展示。

---

### v1.3.5

**📝 日记日期校验与自动补全**

* 日记正文强制要求以日期开头。
* 若首行缺少日期，自动在正文前补全日期标题。

---

### v1.3.4

**🐛 静默时段调度修复**

* 修复开启静默时段后，调度器在静默时段外也不思考的问题。
* 调度器休眠计算现在会加入静默结束时间，确保及时醒来开始思考。

---

### v1.3.3

**🧹 Legacy 清理 + DayFlow 联动修复**

**1. 🧹 Legacy 清理**

* 移除 `inject_mood_into_reply` 旧字段兼容回退。
* 移除 `{last_awareness}` 旧占位符兼容替换。
* 修复版本号不一致问题。
* 修复 `webui.py` 中硬编码版本号。
* 更新 README 移除已废弃配置项说明。

**2. 🔗 思考时读取 DayFlow 日程链路修复**

* 修复思考链路中 `target_date` 参数可能为空的问题。
* 确保调用日程接口时 `target_date` 参数稳定可用。

---

### v1.3.2

**🛠️ Dayflow 依赖识别与自动日记防刷屏修复**

**1. 🔗 Dayflow 依赖探测补强**

* 修复可能把已加载的 Dayflow 误判为"未检测到插件"的问题。
* 依赖绑定改为遍历多个候选实例并按接口验真。
* 兼容通过插件实例或 `service` 暴露 `get_life_context` 的实现。

**2. ⛔ 自动日记失败冷却**

* 修复自动日记失败后下一轮调度立刻重复触发的问题。
* 同一人格、同一日期、同一日记触发键失败后会进入冷却期。
* 成功生成或确认已存在日记后，自动清理失败冷却状态。

**3. 🧾 错误文案收敛**

* 将"未检测到插件"收敛为"未检测到可用的 Dayflow 日程实例"。

---

### v1.3.1

**🛠️ 联动修复与版本清理**

**1. 🔗 DayFlow 按日期读取联动修复**

* 获取/补生成日程改为按目标日期读取，不再只依赖系统当前"今天"。
* 修复跨日场景下前一日自动日记误读次日日程的问题。

**2. 📨 自动推送日记消息链修复**

* 修复自动推送日记时的消息链报错。
* 改为使用 AstrBot 标准消息链发送。

**3. 📅 查看日程重复生成防抖**

* `/查看日程` 的读取链路改为统一走 DayFlow 的目标日期上下文接口。
* 降低已有日程被重复触发生成并覆盖的概率。

**4. 🧹 版本遗留标识清理**

* 去除早期用于测试的 `mood_system` 版本后缀显示。
* 修正 WebUI 与健康接口中的旧版本号遗留。

---

### v1.3.0

**🧠 架构整理：人格识别、会话绑定、自动补全与状态持久化全面升级**

**1. 🧩 人格识别升级为多别名匹配**

* 人格识别支持多别名匹配，自动归一化。

**2. 🔗 会话人格活跃映射自动管理**

* 新增会话-人格活跃映射，记录最近使用时间。
* 过期映射自动清理。
* 可配置保留天数和最大条目数。

**3. 🧠 思考 / 日记前可自动补生成今日日程**

* 新增 `reflection_auto_ensure_today_schedule` 开关。
* 开启后，思考或日记生成前检测到日程缺失会自动补生成。
* 手动生成日记时若补生成日程，会在返回结果里明确提示。

**4. 🔁 思考与日记生成支持内部重试**

* 新增重试次数与重试间隔配置项。
* 模型偶发异常时可自动重试。

**5. 🌐 WebUI 支持按人格查询与管理**

* WebUI 接口支持按人格筛选日记和思考流。
* 状态页返回可用人格列表与人格状态。
* "清除今日思考"接口支持指定人格。

**6. 📓 新增 `/今日日记` 指令**

* 快速查看当前人格今天的日记内容。
* 别名：`查看今日日记` / `daymind_diary_today`。
* 只读不生成，没有日记会直接提示。

**7. 🔗 依赖管理重写，DayFlow 兼容更稳**

* 区分新版和旧版 DayFlow 插件。
* 检查实例接口是否完整，避免兼容性问题。
* 新增 `ensure_today_schedule()` 统一封装"缺日程时补生成"逻辑。

**8. 🧹 推送机制简化**

* 去掉 `push_retry_times` 和 `push_retry_delay_seconds` 配置。
* 日记推送改为直接调用 `send_message`。

**9. ⚙️ 状态持久化更可靠**

* 关键流程会主动保存状态。
* 状态文件结构新增日期字段，恢复流程更严格。
* 日记 memory version counter 按保留周期裁剪。

**10. 📝 默认思考 / 日记模板换代**

* 思考模板更聚焦"当下意识切片"。
* 日记模板更强调情绪主线而非时间线。

---

### v1.2.0

**🧠 心情系统上线**

**1. 💜 心情生成与注入**

* 每次思考后自动生成心情标签。
* 心情标签会影响下次思考前的对话风格。
* 支持独立 Provider 模式和内联提取模式。

**2. 🎮 新增心情相关指令**

* `/查看心情`：查看当前人格的心情状态。
* `/今日心情`：查看当前人格今日的心情变化历史。

**3. ⚙️ 心情相关配置项**

* `enable_mood_system`、`inject_mood_into_reply`、`mood_provider_id`、`mood_reference_reflection_count`、`mood_max_history_per_day`、`mood_style_strength`、`mood_allow_sharp_tone`。

---

### v1.1.0

**🧩 多人格分桶与白名单机制上线**

**1. 🧠 思考 / 日记状态改为按人格隔离**

* DayMind 核心运行状态从单份全局状态改为按人格分桶管理。
* 不同人格现在拥有各自独立的今日思考流、当前状态文本、日记生成状态与本地历史记录。
* 本地存储已调整为按人格目录分层，避免不同人格内容混流。

**2. ✅ 新增人格白名单配置**

* 新增 `enabled_personas` 配置项。
* 只有命中白名单的人格才会启用 DayMind，未命中的人格会直接跳过。
* 思考与日记共用同一套白名单，避免 Bot 拥有大量人格时产生无意义调度压力。

**3. ⏱️ 新增思考调度随机抖动**

* 新增 `thinking_interval_jitter_seconds` 配置项。
* 自动思考在固定间隔基础上支持每轮加入随机抖动，用于错开并发峰值。
* 抖动按每轮重新计算，而不是固定偏移，适合多实例 / 多人格并行场景。

**4. 🎮 手动指令改为跟随当前人格**

* `/手动思考`、`/生成日记`、`/清除今日思考` 均会基于当前会话解析人格。
* 若当前人格未命中白名单，会直接跳过并提示。

**5. 📓 历史日记参考改为人格内读取**

* 日记生成时只参考当前人格自己的历史日记目录。
* 同一个 Bot 下的不同人格可以保持各自连续的叙事线。

**6. ⚙️ 配置界面字段补充**

* 已在 `_conf_schema.json` 中加入人格白名单与思考抖动配置字段。

---

### v1.0.0

**🧠 首次发布：让 Bot 拥有"今天"的状态感**

**1. 🧠 自动思考链路上线**

* 支持按设定间隔自动生成当下思考。
* 思考会综合参考当前日程、最近对话、人格设定与最近思考内容。
* 已加入本地近似去重机制。

**2. 📓 自动日记能力上线**

* 支持在指定时间自动生成今日日记。
* 日记会融合今日日程、当日思考、人格设定与最近历史日记。

**3. 🔗 人格与现实信息接入**

* 支持解析当前会话人格名称与人格设定文本。
* 可联动 `astrbot_plugin_life_scheduler` 获取天气、日程、穿着等现实轨迹。

**4. 🧠 LivingMemory 写入与复写保护**

* 支持将生成的日记写入 LivingMemory。
* 重复生成今日日记时，旧 diary memory 标记为 `deleted` 而非物理删除。

**5. 🧷 内容管理基础能力上线**

* 支持日记星标与备注。
* 支持按日聚合的思考流星标与备注。
* 被星标内容不会参与自动轮换删除。
* 支持分别管理日记与思考流的保留策略。

**6. 🌐 DayMind WebUI 基础完成**

* 支持读取内容列表、查看详情、修改星标、修改备注、更新内部配置、清除今日思考流。

**7. 🧹 今日思考流清空能力上线**

* 新增 `/清除今日思考` 指令。
* 执行后清空今日思考缓存、当前状态文本与本地当日 reflections 文件。
